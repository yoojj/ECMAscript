# Node Util
https://nodejs.org/api/util.html



```js
const util = require('util');

// 타입 검사
util.types.isAsyncFunction();
util.types.isPromise();
util.types.isArray();
util.types.isDate();
util.types.isRegExp();
```
